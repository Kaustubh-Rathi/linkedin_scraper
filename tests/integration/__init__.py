"""Live LinkedIn integration tests (require ``linkedin_session.json``).

Tests here are marked ``@pytest.mark.integration`` and use fixtures from
``tests/conftest.py`` (e.g. ``browser_with_session``). Offline unit tests live
under ``tests/unit/``.
"""
